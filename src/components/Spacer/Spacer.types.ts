import { ViewStyle } from 'react-native';

export interface SpacerProps {
  style?: ViewStyle;
  size?: number;
  horizontal?: boolean;
  border?: boolean;
  borderColor?: string;
  borderSize?: number;
}
